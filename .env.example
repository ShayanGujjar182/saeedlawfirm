# Contact form delivery (Web3Forms, client-side submission).
# The access key is public by design and is also inlined in pages/contact.tsx as a fallback,
# so the form works out of the box. Set this only if you want to override the key per environment.
# Get a key at https://web3forms.com using the inbox ahmadbilal2003@gmail.com.
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
